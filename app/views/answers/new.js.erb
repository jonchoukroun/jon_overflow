$('form.answer-form').toggleClass('hidden');
$('a.new-answer').toggleClass('hidden');

$('a.cancel-answer').on('click', function() {
  $('form.answer-form').toggleClass('hidden');
  $('a.new-answer').toggleClass('hidden');
  return false;
});
