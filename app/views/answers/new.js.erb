$('a.new-answer').on('ajax:success', function() {
  $('form.answer-form').toggleClass('hidden');
});

$('#main-content').on('click', '.full-question a.cancel-answer', function(e) {
  e.preventDefault();
  $('form.answer-form').toggleClass('hidden');
});
