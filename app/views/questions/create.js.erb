var container = $('#content.container');
$(container).empty().html(
  $(" <%= escape_javascript(render @question) %> ")
);
$('<a class="btn btn-default" href="/">').text('Back to questions').appendTo(container);

