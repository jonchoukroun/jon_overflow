$('a.question-edit').bind('ajax:success', function() {
  $('form.question-form').toggleClass('hidden');
});

$('#main-content').on('click', '.full-question a.cancel-question', function(e) {
  e.preventDefault();
  $('form.question-form').toggleClass('hidden');
});
