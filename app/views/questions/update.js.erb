$('.question').empty().html(
  "<%= j(render 'questions/question', question: @question) %>"
);

$('form.question-form').toggleClass('hidden');

$('html, body').animate({
  scrollTop: $('#content').offset().top -50
}, 500);
